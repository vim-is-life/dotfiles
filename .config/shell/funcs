# -*- mode: sh; -*-
# vim: set filetype=sh:

m() {
    # man -Tpdf "$@" | devour zathura -
    man -Tpdf "$@" | zathura -
}

# TODO move change brightness scripts into here to make them fns

# TODO evaluate what other things i should make as functions instead of scripts
